# A function for assessing the performance of each algorithm through cross validation
# through running each algorithm several times (leaving part of the data out) and assesses
# cross validation error.
# our implementation of cv, so we can be extended it


crossValid <- function(algorithmFunction, featureMatrix, outputY){
  
  
  
  
  
  
  
  
  
  
  
  return(list(cvError))
}